package com.bwc.messaging.sms.config;

import org.springframework.boot.test.context.TestConfiguration;

// Disabled test configuration to avoid conflicts with main datasource configs
@TestConfiguration
public class TestDatabaseConfig {
    // Configuration disabled - using main datasource configurations
}